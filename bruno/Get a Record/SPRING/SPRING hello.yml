info:
  name: SPRING hello
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCALHOST}}/api/hello"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
